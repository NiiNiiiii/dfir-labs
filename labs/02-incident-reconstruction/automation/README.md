# Automation — SOAR

## Trigger
- When an incident is **created**
- Condition: incident is related to `LAB02 - Encoded PowerShell Execution`

## Actions
1) Add tag: `lab02`
2) Escalate severity: `High`

> Note: the publish-safe export does **not** include owner assignment to avoid leaking user identifiers.
